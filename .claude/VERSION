{
  "version": "2.9.16",
  "type": "coc-use-template",
  "description": "COC USE template for Python/Ruby developers using Rust-backed Kailash bindings",
  "released": "2026-04-27",
  "upstream": {
    "name": "kailash",
    "type": "coc-source",
    "version": "2.9.0",
    "build_version": "3.20.0",
    "synced_at": "2026-04-26T01:00:00Z",
    "sdk_packages": {
      "kailash-value": "3.20.0",
      "kailash-core": "3.20.0",
      "kailash-macros": "3.20.0",
      "kailash-nodes": "3.20.0",
      "kailash-nodes-enterprise": "3.20.0",
      "kailash-plugin": "3.20.0",
      "kailash-plugin-guest": "3.20.0",
      "kailash-plugin-macros": "3.0.0",
      "kailash-capi": "3.20.0",
      "eatp": "0.1.0",
      "kailash-dataflow": "3.20.0",
      "kailash-nexus": "3.20.0",
      "kailash-kaizen": "3.20.0",
      "kailash-enterprise": "3.20.0",
      "kailash-mcp": "3.20.0",
      "kailash-auth": "3.20.0",
      "kailash-python": "3.20.0",
      "kailash-node": "0.1.0",
      "kailash-wasm": "0.1.0",
      "kailash-ruby": "3.20.0",
      "kailash-cli": "3.20.0",
      "kailash-marketplace": "3.20.0",
      "trust-plane": "3.20.0",
      "kailash-governance": "0.1.0",
      "kailash-pact": "0.1.0",
      "kaizen-agents": "3.20.0",
      "kailash-rl": "3.20.0",
      "kailash-align-serving": "3.20.0",
      "kailash-ml": "3.20.0",
      "kailash-ml-core": "3.20.0",
      "kailash-ml-linalg": "3.20.0",
      "kailash-ml-preprocessing": "3.20.0",
      "kailash-ml-linear": "3.20.0",
      "kailash-ml-tree": "3.20.0",
      "kailash-ml-ensemble": "3.20.0",
      "kailash-ml-boost": "3.20.0",
      "kailash-ml-svm": "3.20.0",
      "kailash-ml-neighbors": "3.20.0",
      "kailash-ml-cluster": "3.20.0",
      "kailash-ml-decomposition": "3.20.0",
      "kailash-ml-metrics": "3.20.0",
      "kailash-ml-selection": "3.20.0",
      "kailash-ml-pipeline": "3.20.0",
      "kailash-ml-text": "3.20.0",
      "kailash-ml-misc": "3.20.0",
      "kailash-ml-explorer": "3.20.0",
      "kailash-ml-nodes": "3.20.0",
      "kailash-ml-python": "3.20.0"
    }
  },
  "changelog": [
    {
      "version": "2.9.16",
      "date": "2026-04-27",
      "summary": "PATCH: track .claude/hooks/lib/ in git. Prior .gitignore had `lib/` ignore with whitelist pinned to the legacy `scripts/hooks/lib/` path, leaving the v2.9.x canonical `.claude/hooks/lib/` payload (7 sibling modules: env-utils, learning-utils, workspace-utils, version-utils, release-drift, runtime, template-resolver) untracked. Downstream consumers that cloned post-v2.9.0 saw an empty `.claude/hooks/lib/` dir; CC startup hooks crashed with 'Cannot find module ./lib/<sibling>'. Fix: replaced `!scripts/hooks/lib/` whitelist with `!.claude/hooks/lib/` (legacy `scripts/hooks/` is obsoleted via .coc-obsoleted from v2.9.1).",
      "changes": [
        "GLOBAL .gitignore: removed `!scripts/hooks/lib/` whitelist (legacy path obsoleted); added `!.claude/hooks/lib/` so the v2.9.x sibling modules are tracked.",
        "GLOBAL .claude/hooks/lib/: first commit of the 7 sibling modules to git history. Files have been on working tree since v2.9.0 sync but were masked from `git add` by the gitignore."
      ]
    },
    {
      "version": "2.9.15",
      "date": "2026-04-26",
      "summary": "Patch follow-up to 2.9.14 \u2014 close cross-SDK gap by re-distributing rs variant overlays after Gate 1 extended variants/rs/rules/agents.md (+'Verify Specialist Tool Inventory' MUST) and variants/rs/rules/ci-runners.md (+Rules 9/10/11: Binding-CI paths-ignore for doc-only / Workflow Crons cost footer / Release PRs skip PR-gate). Both rules re-composed via .claude/bin/compose.mjs (--global rules/X.md --overlay variants/rs/rules/X.md). rs USE templates now carry the same Tool Inventory delegation discipline + GitHub-Actions-cost rules that py templates received in 2.9.14 (Gate-1 omission noted in 2.9.14 audit observations). No SDK pin changes (rs template pyproject.toml binding-wheel pin unchanged).",
      "changes": [
        "RS-VARIANT rules/agents.md re-composed: now contains 'MUST: Verify Specialist Tool Inventory Before Implementation Delegation' (read-only specialists like security-reviewer, analyst, reviewer MUST NOT be delegated implementation work). Examples table for 17+ specialists landed via global slot.",
        "RS-VARIANT rules/ci-runners.md re-composed: added Rule 9 'Binding-CI paths-ignore Covers ALL Doc-Only Surfaces' + Rule 10 'Workflow Crons MUST Have Explicit Cost Footer' + Rule 11 'Release PRs MUST Skip The PR-Gate Suite' BEFORE 'MUST NOT Rules' section. Numbered Rules 9-11 (rs variant) parallel global Rules 6a/7/8 \u2014 same content, different numbering due to rs variant's pre-existing \u00a76/\u00a77/\u00a78 (Zombie-Job, Idle-Not-Accepting, Tag-Gated Release).",
        "VERSION: 2.9.14 \u2192 2.9.15.",
        "HOOKS: 12/12 verified."
      ]
    },
    {
      "version": "2.9.14",
      "date": "2026-04-26",
      "summary": "Sync loom 2.9.0 \u2014 PY CYCLE 8 + RS CYCLE 5 dual-SDK convergence. RS-variant overlay applied. 4 rule files updated: rs-variant rules/agents.md (no change in this cycle \u2014 variant carries Tool-Inventory-equivalent specialist delegation block; global Tool Inventory MUST landed in py templates only by Gate 1 classification), global rules/build-repo-release-discipline.md (added MUST Rule 5 PR Review Sub-Package Version Bump Sweep \u2014 applies to Cargo.toml + lib.rs version atomicity), global rules/git.md (added Release-Prep release/v* convention + Pre-FIRST-Push CI Parity Discipline \u2014 both rs-grounded with PR #602/PR #598 evidence), rs-variant rules/ci-runners.md (no change in this cycle \u2014 variant carries \u00a76/\u00a77/\u00a78 runner-ops content; global Rules 6a/7/8 GitHub Actions cost-aware rules landed in py templates only by Gate 1 classification). 1 rs-variant rule extended: rules/build-speed.md (added MUST 'Run Full CI Job-Set Locally Before FIRST Push AND Before Admin-Merge'). 1 NEW rs-variant skill: skills/18-security-patterns/rls-security-definer-preauth-carveout.md (517 lines \u2014 typed Rust SecurityDefinerBuilder API doc, kailash-rs v3.21+, issue #571 / kailash-coc-claude-rs#58 ask 2). SDK pins UNCHANGED (rs templates serve Python/Ruby developers consuming kailash-rs through bindings; pyproject.toml pins kailash binding wheel only).",
      "changes": [
        "GLOBAL rules/build-repo-release-discipline.md mirror-applied from loom global (Rule 5 PR Review Sub-Package Version Bump Sweep). Cross-SDK applicable: same Cargo.toml + lib.rs version atomicity in kailash-rs.",
        "GLOBAL rules/git.md mirror-applied from loom global (Release-Prep release/v* + Pre-FIRST-Push CI Parity). Origin: kailash-rs PR #602 + PR #598.",
        "RS-VARIANT rules/agents.md re-applied from variants/rs/rules/agents.md (no semantic delta this cycle; rs variant retains its Specialist Delegation block which is structurally similar to the new global Tool Inventory MUST).",
        "RS-VARIANT rules/ci-runners.md re-applied from variants/rs/rules/ci-runners.md (no semantic delta this cycle; rs variant retains its \u00a76 Zombie-Job + \u00a77 Idle-But-Not-Accepting + \u00a78 Tag-Gated Release runner-ops MUSTs which are orthogonal to the new global 6a/7/8 GitHub-Actions-cost rules).",
        "RS-VARIANT rules/build-speed.md re-applied from variants/rs/rules/build-speed.md (added MUST 'Run Full CI Job-Set Locally Before FIRST Push AND Before Admin-Merge' \u2014 five-command checklist: cargo +nightly fmt, cargo +1.95 clippy --workspace --all-targets -- -D warnings, cargo nextest run --workspace, RUSTDOCFLAGS=-Dwarnings cargo doc default-features, RUSTDOCFLAGS=-Dwarnings cargo doc --no-default-features).",
        "NEW (rs variant) skills/18-security-patterns/rls-security-definer-preauth-carveout.md (517 lines, copied from kailash-rs source-of-truth). Adds rs-specific 'Using the builder (kailash-rs only)' section documenting typed Rust SecurityDefinerBuilder API. Documents four-statement emission, T7 timing-close reminder enforcement, T8 multi-tenant-filter typed-error contract, identifier-safety routing through sql_safety::validate_identifier.",
        "SDK PINS: UNCHANGED (rs templates pin kailash binding wheel via pyproject.toml; Rust-toolchain-specific pins are not managed by /sync per agent contract). pyproject.toml kailash>=3.20.0 retained.",
        "HOOKS: registered settings.json hooks verified on disk."
      ]
    },
    {
      "version": "2.9.13",
      "date": "2026-04-20",
      "summary": "Rule restructuring (cross-template): orphan-detection.md + agents.md trimmed; ci-runners.md now variant-rs (retains \u00a76/\u00a77 with rs-specific LaunchAgent identifiers); new skill files orphan-audit-playbook + worktree-orchestration.",
      "changes": [
        "MODIFIED (global): rules/orphan-detection.md \u2014 366L \u2192 198L. Phase 5.11 narrative + full Detection Protocol + per-rule origin histories extracted to skills/16-validation-patterns/orphan-audit-playbook.md. Rule file now holds only load-bearing MUST clauses.",
        "UNCHANGED (variant-rs overlay): rules/agents.md \u2014 rs template already carries the 194L rs variant (variants/rs/rules/agents.md) which includes Parallel-Worktree Package Ownership. Global agents.md trim landed upstream but does not affect rs template because variant overlay wins.",
        "MODIFIED (variant-rs): rules/ci-runners.md \u2014 now sourced from variants/rs/rules/ci-runners.md (258L). Global ci-runners.md trimmed to 170L at loom; \u00a76 Zombie-Job Cancellation Protocol + \u00a77 Idle-But-Not-Accepting Runner Protocol retained in rs variant with concrete `esperie-enterprise/kailash-rs` runner identifiers (Jacks-Mac-Studio, Esperies-Mini, esperie-mac) and launchctl kickstart invocations. Content otherwise identical to prior rs template.",
        "NEW (skill): skills/16-validation-patterns/orphan-audit-playbook.md \u2014 72 lines. Phase 5.11 TrustAwareQueryExecutor post-mortem + 6-step Detection Protocol + per-rule origin histories.",
        "NEW (skill): skills/30-claude-code-patterns/worktree-orchestration.md \u2014 55 lines. Extended evidence for rules/agents.md worktree MUSTs + Reviewer Prompts section.",
        "MODIFIED (nav): skills/16-validation-patterns/SKILL.md + skills/30-claude-code-patterns/SKILL.md \u2014 added Audit Playbooks / Detailed Reference entries for the new skill files.",
        "NEW (variant mirror): variants/rs/rules/ci-runners.md \u2014 258 lines, mirrors loom variant authority into template's variants tree for traceability.",
        "toml parse: pyproject.toml verified via tomllib.loads (no SDK pin changes this cycle).",
        "HOOKS: all registered settings.json hooks verified on disk.",
        "VERSION: template 2.9.12\u21922.9.13; upstream SDK pins unchanged."
      ]
    },
    {
      "version": "2.9.12",
      "date": "2026-04-20",
      "summary": "Sync rs proposal (v3.20.0 cycle) \u2014 8 rule additions distributed: 4 global (orphan-detection \u00a74a/\u00a74b/\u00a75a/\u00a77/\u00a78 present after overwrite, specs-authority \u00a75b, git Commit-Message Claim Accuracy, autonomous-execution \u00a74 Fix-Immediately, ci-runners \u00a76 Zombie-Job + \u00a77 Idle-But-Not-Accepting, testing Pytest Plugin + Test-Skip Triage + Env-Var) + 4 rs-variant (build-speed Match CI Clippy Toolchain, dataflow-null-typing NEW, release NEW, agents Parallel-Worktree Package Ownership). Cargo SDK pins bumped 3.19.0\u21923.20.0 across workspace members (pyproject.toml kailash-enterprise>=3.19.0 \u2192 >=3.20.0). Also landed this morning's py-cycle globals via overwrite.",
      "changes": [
        "PIN: pyproject.toml \u2014 kailash-enterprise>=3.19.0 \u2192 >=3.20.0 (kailash-rs v3.20.0 release).",
        "EXTENDED (global overwrite): rules/orphan-detection.md \u2014 template 270L \u2192 366L. Added \u00a74a 'Stub Implementation MUST Sweep Deferral Tests In Same Commit' (+28 lines), \u00a74b 'Error-Contract Refactor MUST Sweep Paired Tests In Same Commit' (+33 lines), \u00a75a 'Collect-Only Gate Passes Per-Package, Not Combined Root Invocation' (+30 lines). From py-cycle codify 2026-04-20.",
        "EXTENDED (global overwrite): rules/specs-authority.md \u2014 template 194L \u2192 227L. Added \u00a75b 'Spec Edits MUST Trigger Full Sibling-Spec Re-Derivation' (+32 lines). From py-cycle codify 2026-04-19/20.",
        "EXTENDED (global overwrite): rules/git.md \u2014 template 126L \u2192 162L. Added \u00a7Commit-Message Claim Accuracy (+35 lines). Origin: kailash-rs 2026-04-20 self-correction of commit 4c400a93 claiming a diff it didn't contain.",
        "EXTENDED (global overwrite): rules/autonomous-execution.md \u2014 template 123L \u2192 161L. Added \u00a74 Fix-Immediately When Review Surfaces A Same-Class Gap Within Shard Budget (+37 lines). Origin: kailash-rs 2026-04-20 PR #435\u2192#437 cycle.",
        "EXTENDED (global overwrite): rules/ci-runners.md \u2014 template 170L \u2192 258L. Added \u00a76 Zombie-Job Cancellation Protocol (+39 lines) + \u00a77 Idle-But-Not-Accepting Runner Protocol (+48 lines). Origin: kailash-rs 2026-04-20 Jacks-Mac-Studio + Esperies-Mini self-hosted runner incidents during v3.20.0/v3.20.1 release cycles. Language-agnostic \u2014 abstracted from rs-variant classification suggestion by Gate 1.",
        "EXTENDED (rs variant): rules/testing.md \u2014 loom rs variant 202L \u2192 extended with three py-cycle sections adapted for binding consumer projects (Env-Var Test Isolation via monkeypatch + threading.Lock, Pytest Plugin + Marker Declaration Pair, Test-Skip Triage Decision Tree). Template now matches updated rs variant authority.",
        "EXTENDED (rs variant): rules/agents.md \u2014 loom rs variant 150L \u2192 extended with Parallel-Worktree Package Ownership Coordination (+45 lines). Orchestrator designates ONE agent as version owner per sub-package; sibling agents explicitly excluded from pyproject.toml/Cargo.toml/CHANGELOG.md edits. Cross-SDK from py 2026-04-20 three-agent parallel-release session.",
        "EXTENDED (rs variant): rules/build-speed.md \u2014 template 142L \u2192 178L (full loom rs variant authority). Added MUST: Match CI Clippy Toolchain Version (+36 lines) \u2014 Rust 1.95.0 tightens clippy::doc_markdown on bare technology names; cargo +<ci-version> clippy or rust-toolchain.toml pin. Origin: kailash-rs PR #437 v3.20.0 cycle.",
        "NEW (rs variant): rules/dataflow-null-typing.md \u2014 154 lines, kailash-rs specific. Four MUST rules: (1) Every Value::Null binding routes through type-aware helper, (2) sqlx 0.8.6 Real/Double NULL-type swap bug documented inline, (3) SQL-cast + FieldType paths share one SqlTypeHint enum, (4) user-SQL parsers clamp at MAX_PARAMS=65535. Covers sqlx Any driver statement-cache corruption and the 0xc7 0x0a UTF-8 poisoning failure mode. Origin: kailash-rs#424 + PR #435 + PR #437 (v3.20.0).",
        "NEW (rs variant): rules/release.md \u2014 52 lines. MUST: NEVER Round-Trip Wheels Through actions/upload-artifact In The Release Workflow. Each platform job MUST build \u2192 audit \u2192 twine upload --skip-existing in-job; fan-in reduced to GitHub Release creation only. Origin: kailash-rs 2026-04-20 v3.20.0 release failed with 'Artifact storage quota has been hit'; commit a4d648d4 regressed the in-job-publish invariant (669e7cfb).",
        "VARIANTS MIRROR: loom rs variant files mirrored into template's .claude/variants/rs/rules/ for reference: build-speed.md, testing.md, agents.md, dataflow-null-typing.md, release.md (5 files).",
        "HOOKS: 12/12 registered settings.json hooks exist on disk (verified).",
        "upstream.build_version 3.19.0 \u2192 3.20.0; sdk_packages map updated across workspace members. Independent-version crates preserved: eatp=0.1.0, kailash-governance=0.1.0, kailash-pact=0.1.0, kailash-plugin-macros=3.0.0, kailash-node=0.1.0, kailash-wasm=0.1.0."
      ]
    },
    {
      "version": "2.9.11",
      "date": "2026-04-19",
      "summary": "Drift reconciliation per zero-tolerance Rule 1 \u2014 4 pre-existing template items fixed: (1) rules/framework-first.md rs variant gained 'Framework Version-Stable Integration \u2014 Drive The Data, Not The Dispatch' section (was merged into template at 2.8.19 sync but never propagated into the rs variant file itself, causing the template to drift from the variant authority); (2) rules/security.md rs variant gained Sanitizer Contract (\u00a71/\u00a72/\u00a73) + Multi-Site Kwarg Plumbing sections (same drift shape as #1); (3) rules/nexus-webhook-hmac.md template Origin line synced to current global (references extractor-trait architecture rework kailash-rs#404 S1-S8 now that S1-S7 have merged); (4) rules/event-payload-classification.md \u2014 MISSING from template entirely since its 2.8.12 manifest addition, now copied in from global. Loom rs variants updated to remain authoritative.",
      "changes": [
        "rules/framework-first.md \u2014 rs variant +36 lines (Framework Version-Stable Integration section). Template already had it from prior sync; this closes the drift between variant and template.",
        "rules/security.md \u2014 rs variant +86 lines (Sanitizer Contract + Multi-Site Kwarg Plumbing sections). Template already had these from prior sync; this closes the drift and deduplicates the MUST NOT block the earlier merge left orphaned.",
        "rules/nexus-webhook-hmac.md \u2014 Origin line synced to reference extractor-trait rework kailash-rs#404 S1-S8 (S1-S7 merged 2026-04-18 through 2026-04-19).",
        "rules/event-payload-classification.md \u2014 NEW in rs template (was missing since loom 2.8.12 manifest addition). Cross-SDK event payload safety rule covering record_id hashing + fields_changed partitioning.",
        "VERSION: 2.9.10\u21922.9.11; upstream.version 2.8.21\u21922.8.22; upstream.synced_at bumped."
      ]
    },
    {
      "version": "2.9.10",
      "date": "2026-04-19",
      "summary": "Parity sync from loom 2.8.21 \u2014 propagates 3 cross-SDK rules authored in kailash-py's 2026-04-19 parallel-shard codify: rules/worktree-isolation.md Rule 4 (parent-checkout path ban) + Rule 5 (incremental commit discipline) flow from global; rules/agents.md reviewer-mechanical-sweep section added to the rs variant overlay and flows from there. No SDK pin changes this cycle.",
      "changes": [
        "EXTENDED (global): rules/worktree-isolation.md \u2014 +Rule 4 'Worktree Prompts MUST NOT Reference The Parent-Checkout Path' + Rule 5 'Worktree Agents MUST Commit Incremental Progress' (+68 lines). Rule 4 prohibits absolute paths pointing to the PARENT checkout (e.g. /Users/.../kailash-py/packages/...) which silently resolve to main despite isolation=worktree; relative paths or absolute paths anchored to the pinned worktree path are fine (reconciles with Rule 1). Rule 5 mandates explicit 'commit after each milestone' in the agent prompt + orchestrator verification of \u22651 commit on the branch before declaring work landed; worktrees with zero commits auto-clean, losing 100% of output. Cross-SDK: Rust ml-specialist / dataflow-specialist / kaizen-specialist agents face the same write-to-main leak. Origin: kailash-py 2026-04-19 GPU-first Phase 1 parallel-shard session (2 of 3 ml-specialist shards wrote to main; 1 lost 300+ LOC to auto-cleanup).",
        "EXTENDED (rs variant): rules/agents.md \u2014 +'MUST: Reviewer Prompts Include Mechanical AST/Grep Sweep, Not Only Diff Review' section (after Quality Gates background-agent block, +32 lines). Every gate-level reviewer prompt MUST enumerate mechanical sweeps (parity grep, collect-only, pip/cargo check, __all__ verification) that verify ABSOLUTE state before LLM judgment on the diff. LLM review of the diff catches what's wrong with the new code; mechanical sweeps catch what's missing from OLD code that the spec also touched (the orphan failure mode). Placed in rs variant (not global) because rs variant has narrower specialist list + rs-specific cargo-check sweep example; global went to py via its own variant overlay. Origin: kailash-py 2026-04-19 \u2014 reviewer APPROVED kailash-ml 0.12.0 but /redteam mechanical sweep caught 2 of 7 return sites missing required field.",
        "SDK PINS: unchanged (no rs SDK release this cycle; pyproject.toml kailash-enterprise>=3.19.0 unchanged).",
        "HOOKS: 12/12 registered settings.json hooks exist on disk (verified).",
        "PRE-EXISTING DRIFT NOTED (not touched this cycle): rules/framework-first.md, rules/security.md \u2014 2.8.19 sync intentionally merged global extensions into rs variants at sync time; loom rs variant files remain at pre-2.8.19 content so sync-time re-resolution shows drift. Matches documented 2.8.19 changelog behavior. rules/nexus-webhook-hmac.md has a 1-line Origin-update drift from 2.8.17. rules/event-payload-classification.md missing from template (added to manifest at loom 2.8.12 but rs-template sync missed it). These are all prior-cycle artifacts; parity scope for this cycle is the 3 rules above only."
      ]
    },
    {
      "version": "2.9.9",
      "date": "2026-04-19",
      "summary": "Gate 1 classification of kailash-rs v3.18.0 post-release proposal (8 entries: 6 already codified in prior loom cycles, 2 new content writes). SDK pin bump 3.18.0\u21923.19.0 (workspace).",
      "changes": [
        "EXTENDED (rs variant): rules/build-speed.md \u2014 +section 'MUST: Use Nightly Rustfmt To Match CI' (entry 7, rs-variant). CI Format job uses dtolnay/rust-toolchain@nightly; rustfmt.toml enables nightly-only options (imports_granularity, group_imports, trailing_comma, blank_lines_upper_bound, blank_lines_lower_bound, format_strings) that stable silently ignores. Local `cargo fmt --all` passes but CI rejects. Required setup: `rustup toolchain install nightly --profile minimal --component rustfmt`. BLOCKED rationalizations enumerated. Origin: three PRs (#427, #428, #430) failed CI fmt while code was correct (2026-04-19).",
        "EXTENDED (global): rules/orphan-detection.md \u2014 +Rule 7 'Public-API Removal MUST Sweep Binding / Downstream Consumers In The Same PR' + Rule 8 'Delete-or-Migrate Grep Is A Merge Gate' (entry 8, global extension). Generalizes kailash-py's Rule 4 (test sweep) to every sibling consumer tree (bindings/*, packages/*, examples/, ffi/, clients/). Grep-in-PR-body assertion becomes the merge-gate artifact. Origin: kailash-rs PR #427 (2026-04-19) \u2014 removed kailash-nexus::auth re-exports with clean cargo check but Ruby + Python + Node CI all red on first run.",
        "NO-OP: 6 other proposal entries (binding-feature-gate-consistency, issue template cross-sdk, cross-SDK verification, nexus extractors, nexus http status convention, worktree isolation) already codified in prior loom cycles (2.8.13\u20132.8.19); verified by Gate 1 and skipped.",
        "PIN: pyproject.toml \u2014 kailash-enterprise>=3.18.0 \u2192 >=3.19.0 (kailash-rs workspace version bump).",
        "upstream.build_version 3.18.0 \u2192 3.19.0; sdk_packages map updated across workspace members. Independent-version crates preserved: eatp=0.1.0, kailash-governance=0.1.0, kailash-pact=0.1.0, kailash-plugin-macros=3.0.0, kailash-node=0.1.0, kailash-wasm=0.1.0.",
        "RS VARIANT NOTE: orphan-detection.md has no rs variant in sync-manifest.yaml \u2014 extension merged into template as-is. Rules 7+8 apply to Python/Ruby binding consumers of Rust crates (the rs USE template audience). build-speed.md is rs-variant per sync-manifest.yaml line 338 \u2014 nightly-rustfmt section landed in variant overlay."
      ]
    },
    {
      "version": "2.9.8",
      "date": "2026-04-19",
      "summary": "Sync loom 2.8.19 \u2014 7 global rule extensions from kailash-py round-10 post-release audit codify (cross-SDK, language-agnostic patterns). No rs SDK release; Cargo pins unchanged (3.18.0).",
      "changes": [
        "EXTENDED: rules/tenant-isolation.md \u2014 +Rule 3a 'Keyspace Version Bumps Require Invalidation-Path Sweep' (CRITICAL). When CacheKeyGenerator default version bumps (v1\u2192v2), every invalidation entry point MUST use a version-wildcard pattern (`dataflow:v*:...`) to sweep both legacy and current keys. BLOCKED rationalizations + origin (kailash-py PR #522/#529 fast-patch, dataflow 2.0.12).",
        "EXTENDED: rules/security.md \u2014 +'Sanitizer Contract \u2014 DataFlow Display Hygiene' section (3 sub-rules: token-replace not quote-escape, type-confusion MUST raise, safe types pass through) + 'Multi-Site Kwarg Plumbing' section (security-relevant kwargs plumbed through helpers MUST be updated at every call site in the same PR). Rs-variant Rust clauses (Credential Comparison, Fail-Closed Defaults, Network Transport Hardening) preserved.",
        "EXTENDED: rules/orphan-detection.md \u2014 +Rule 6 'Module-Scope Public Imports Appear In __all__' (HIGH). Any symbol imported at module-scope into a package's __init__.py MUST appear in __all__. Evidence: kailash-ml 0.11.0 eagerly imported DeviceReport + device_report_from_backend_info + device + use_device but omitted all four from __all__; patched in 0.11.1. USE-NOTE: Python-specific __all__ pattern. The rs USE template serves Python/Ruby binding consumers per sync-manifest.yaml lines 60-63, so Python examples land correctly.",
        "EXTENDED: rules/cross-sdk-inspection.md \u2014 +\u00a73a 'Structural API-Divergence Disposition' (when sibling SDKs diverge structurally, close ticket with BOTH Tier-2 binding-path test + signature-invariant test, same PR; `Python doesn't have this API` closures are BLOCKED) + \u00a75 'Cross-SDK Symbol Citations MUST Be Grep-Verified' (every concrete symbol cited across SDKs verified against working tree before commit; evidence in commit body). Origin: kailash-py issue #525 / PR #528.",
        "EXTENDED: rules/dependencies.md \u2014 +'Phantom Transitive Deps \u2014 Resolve Via Lockfile Upgrade, Not Local Caps' section (multi-ecosystem: uv + cargo + npm). Phantom transitive dep (installed by lockfile, zero-imports) holding a secondary package at an old cap MUST be resolved by `uv lock --upgrade-package X` / `cargo update -p X`, NOT by manifest-level caps. Origin: kailash-py PR #530 \u2014 google-generativeai 0.8.6 phantom blocked protobuf upgrade; dropped cleanly.",
        "EXTENDED: rules/agents.md \u2014 Quality Gates table +'After release (post-merge audit)' row (RECOMMENDED reviewer run against merged main; if CRIT/HIGH surfaces, patch branch in SAME session and ship as x.y.z+1) + 'Verify Agent Deliverables Exist After Exit' section (parent orchestrator MUST ls/Read claimed files before trusting agent completion; budget-exhaustion-mid-message truncates final writes). Rs-variant narrower specialist list preserved.",
        "EXTENDED: rules/framework-first.md \u2014 +'Framework Version-Stable Integration \u2014 Drive The Data, Not The Dispatch' section. Integrations MUST iterate registered-handlers data structure (on_startup / on_shutdown lists) NOT call dispatch method by name (startup() / shutdown()); dispatch names drift across framework versions. Origin: kailash-py issue #531 / PR #533 \u2014 kailash-nexus 2.1.0 crashed at uvicorn lifespan because FastAPI exposed only _startup; 2.1.1 fix iterated on_startup list. Rs-variant binding-focused anti-pattern table preserved.",
        "PIN: Cargo workspace unchanged (3.18.0). No rs SDK release in this cycle; pyproject.toml kailash-enterprise>=3.18.0 unchanged.",
        "HOOKS: 12/12 registered settings.json hooks exist on disk.",
        "RS VARIANT NOTE: 3 of 7 files (security.md, agents.md, framework-first.md) have rs variants per sync-manifest.yaml. New 2.8.19 sections were MERGED into the existing rs variants (not overwrites) to preserve rs-variant content: Rust security clauses, narrower specialist list, binding-focused anti-pattern table. The 4 no-rs-variant files (tenant-isolation, orphan-detection, cross-sdk-inspection, dependencies) overwritten from global as-is."
      ]
    },
    {
      "version": "2.9.7",
      "date": "2026-04-19",
      "summary": "Single-file body-shape correction (loom 2.8.18) \u2014 skills/03-nexus/nexus-http-status-convention.md rewritten from {error, status} to {error, code} shape. No SDK pin change.",
      "changes": [
        "Reconcile nexus-http-status-convention skill body shape ({error, status} \u2192 {error, code}) to match SDK contract verified by red-team grep against 8 production refs in crates/kailash-nexus/src/. Companion to 2.8.17 rule."
      ]
    },
    {
      "version": "2.9.6",
      "date": "2026-04-19",
      "summary": "Sync loom 2.8.16 + 2.8.17 (2-version catch-up) \u2014 2 new global rules (worktree-isolation, nexus-http-status-convention) + 1 new global skill (nexus-extractors) + 3 extended global rules (orphan-detection step 3 bridge-shim, observability LogRecord reserved-attr, dataflow-identifier-safety Rule 6 primitive-vs-orchestrator). variants/py overlays for nexus-specialist + deployment.md NOT applied to rs target. Rs variant rules unchanged (test-skip decision tree extension to global testing.md NOT propagated to rs variant). SDK pin unchanged (3.18.0).",
      "changes": [
        "NEW RULE: rules/worktree-isolation.md \u2014 path-scoped to .claude/agents/** + .claude/commands/** + **/agents/**. 3 MUST clauses: (1) orchestrator pins absolute worktree path + verify instruction; (2) specialist agents self-verify cwd + git branch at Step 0; (3) parent verifies deliverables exist after agent exit. BLOCKED rationalizations enumerated. Origin: kailash-py session 2026-04-19 ml/dataflow/kaizen specialist drift PRs #502-#508 (loom 2.8.16).",
        "NEW RULE: rules/nexus-http-status-convention.md \u2014 path-scoped to **/nexus/** + **/*handler* + **/*endpoint*. 5 MUST clauses freezing the NexusError variant \u2192 HTTP status mapping (12-row taxonomy), JSON error body shape ({error, code}), 5xx-vs-502/503/504 hard rule, extractor-based handler typed-status returns, legacy InvalidInput\u2192400 mapping. Cross-references handler-status-codes + nexus-extractors + nexus-webhook-hmac. Origin: kailash-rs#404 S8 (loom 2.8.17).",
        "NEW SKILL: skills/03-nexus/nexus-extractors.md \u2014 HIGH-priority extractor surface reference (Headers, Bytes, BodyStream, Path, Query, State, Extension, TypedHeader, Json) + response shapes (plain dict, tuple, triple, None\u2192204, NexusHandlerError) + Ruby keyword-arg blocks + performance notes + backward compatibility. Origin: kailash-rs#404 S1-S7 (loom 2.8.17).",
        "EXTENDED: rules/orphan-detection.md \u2014 Detection Protocol gains step 3 'Bridge-shim verification' (+1 step). Hot-path call sites that route through isolating shims (LegacyHandlerAdapter, CompatBridge, FacadeAdapter) do NOT count as wired \u2014 the new surface is still an orphan until non-shim consumers exist. Evidence: kailash-rs#404 S4a commit 90858bab (loom 2.8.17).",
        "EXTENDED: rules/observability.md \u2014 +42 lines, LogRecord reserved-attribute collision section. structlog/stdlib field name collisions (process, thread, asctime, lineno) shadow the runtime-managed attributes and produce confusing logs. Cross-language: same failure mode in Python logging.LogRecord and Rust tracing::Span (loom 2.8.16).",
        "EXTENDED: rules/dataflow-identifier-safety.md \u2014 +43 lines, new Rule 6 'Primitive-vs-Orchestrator Layer Distinction For Destructive Operations'. Per-DDL primitive callers use force_drop + DropRefusedError; multi-DDL orchestrator callers use force_downgrade / force_remove_columns / force_rollback + their respective *RefusedError types. The two MUST NOT subclass each other \u2014 distinct audit-trail attribution. Origin: kailash-py gh #510 + PR #517, cross-SDK parity with kailash-rs codify (loom 2.8.16).",
        "RS VARIANT NOTE: variants/rs/rules/testing.md NOT updated with the test-skip-discipline triage decision tree that loom added to global rules/testing.md in 2.8.16 \u2014 the rs variant testing rule was last refreshed at 2.3.5 (Python-binding flavoring) and the global extension does not target rs's variant scope. Future rs codify cycle may upstream a rs-flavored equivalent.",
        "VARIANTS NOT APPLIED: variants/py/agents/frameworks/nexus-specialist.md (Python middleware patterns) and variants/py/rules/deployment.md \u2014 py-only overlays from loom 2.8.16 round, not propagated to rs target.",
        "SKIPPED (unchanged): rules/deployment.md (small loom edit, identical to rs template), rs variant rules/agents.md + zero-tolerance.md + testing.md + independence.md + patterns.md + connection-pool.md + framework-first.md + build-speed.md + e2e-god-mode.md + security.md (variant overlays in place; loom rs variants not modified in 2.8.16/2.8.17).",
        "PIN: pyproject.toml kailash-enterprise>=3.18.0 unchanged (rs Cargo workspace remains at 3.18.0; no SDK release between 2.9.5 and 2.9.6 syncs)."
      ]
    },
    {
      "version": "2.9.5",
      "date": "2026-04-19",
      "summary": "Sync loom 2.8.15 (round 2) \u2014 rules/dependencies.md extended with new section 'Declared = Gated Consistently \u2014 Optional Dependencies + Feature Gates' (module gate matches optional-dep gate + downstream feature transitively enables required feature). Authored from kailash-rs#417 proposal with cross-SDK adaptation (Rust Cargo features + Python optional-dependencies DO/DO-NOT pairs). No SDK pin change (rs remains 3.18.0).",
      "changes": [
        "EXTENDED RULE: rules/dependencies.md \u2014 new section after 'Declared = Imported' covering optional-dep feature-gate consistency. Two MUST subclauses: (1) module-scope `use optional_dep::` / `import optional_pkg` requires matching `#[cfg(feature)]` / extras gate; (2) downstream feature that imports from a gated module must transitively require the gate. Rust + Python DO/DO-NOT pairs. BLOCKED rationalizations enumerated. Audit grep snippet included (Rust binding scan + Python module-scope extra-import scan). Origin: kailash-rs#417 (2026-04-19), fix commit d04c098a, journal 0038-DISCOVERY-binding-feature-gate-consistency.md. Line count 190 \u2192 289 (+99).",
        "SKIPPED (build_only per proposal): .github/ISSUE_TEMPLATE/cross-sdk.md and crates/kailash-dataflow/tests/regression_405_upsert_sqli.rs \u2014 BUILD-repo-specific artifacts, not COC-syncable. Rule encoded by cross-sdk.md template is already in loom's global rules/cross-sdk-inspection.md MUST Rule 5 from round-1 sync.",
        "No SDK pin change: rs workspace remains at 3.18.0 (round-1 bump). pyproject.toml kailash-enterprise>=3.18.0 unchanged."
      ]
    },
    {
      "version": "2.9.4",
      "date": "2026-04-19",
      "summary": "Sync loom 2.8.14 \u2014 4 new global skills (test-skip-discipline, dataflow migration-scaffold, nexus handler-status-codes, multi-axis-decomposition) + extended schema-migration.md (\u00a7 7 force_downgrade replaces prior \u00a7 3a force_drop) + new guide 09-parallel-agent-bundles. Variant migrate.py landed in py target only (not rs). SDK pin bumped 3.17.1 \u2192 3.18.0.",
      "changes": [
        "NEW SKILL: skills/test-skip-discipline/SKILL.md \u2014 universal testing discipline. Distinguishes acceptable test.skip (cannot execute: missing creds, wrong platform, feature flag off) from BLOCKED test.skip (system is broken). Grounded in STP chatbot-503 incident (kailash-coc-claude-rs#52) where test.skip(chatStatus >= 500) silently masked broken Bedrock for unknown period. 429 retry-then-fail edge case. /redteam grep protocol.",
        "NEW SKILL: skills/02-dataflow/migration-scaffold.md \u2014 copy-pasteable starter for numbered DataFlow migrations. File naming convention, up/down structure, mandatory dialect.quote_identifier() per dataflow-identifier-safety.md MUST Rule 1, force_downgrade gate per schema-migration.md \u00a7 7, Tier 2 regression-test requirement. Two worked examples (add-column-with-classified-default, destructive-downgrade).",
        "NEW SKILL: skills/03-nexus/handler-status-codes.md \u2014 opinionated HTTP status code selection guide for Nexus handlers. The hard rule '5xx = us, 502/503/504 = upstream', LLM-deployment-preset example (Bedrock ThrottlingException \u2192 429; Bedrock 5xx \u2192 503; AuthError::Missing \u2192 500), observability tie-in (5xx MUST emit WARN with upstream_status + upstream_reason, auth-header content pre-masked). Cross-references test-skip-discipline.",
        "NEW SKILL: skills/13-architecture-decisions/multi-axis-decomposition.md \u2014 architectural meta-pattern. When SDK abstraction lumps multiple independent concerns into one identifier (monolithic enum, provider name, backend type), adding the 11th variant is exponentially harder than the 10th. Three-reason rejection rationale (closed set / no type safety / no composition), 5-step application protocol, related decisions across PACT envelope, DataFlow dialect, Kaizen signature.",
        "NEW GUIDE: guides/deterministic-quality/09-parallel-agent-bundles.md \u2014 session-pattern guide for non-shard follow-up bundles. When-to-use vs when-NOT-to-use criteria (independence, \u2264~150 LOC per item, no cross-item invariants, feedback loops fire per agent), decision matrix, dispatch mechanics with worktree-isolation, dependency serialization, cross-references to autonomous-execution.md \u00a7 Per-Session Capacity Budget.",
        "EXTENDED RULE: rules/schema-migration.md \u2014 prior \u00a7 3a force_drop replaced with new \u00a7 7 force_downgrade (rename disambiguates orchestrator-layer flag from primitive-DDL-layer force_drop in dataflow-identifier-safety.md MUST Rule 4). MigrationManager.apply_downgrade(*, force_downgrade=False) Python signature, MigrationManager::rollback(version, dataflow, force_downgrade) Rust signature. DowngradeRefusedError (Python) / DataFlowError::DowngradeRefused (Rust). Expanded BLOCKED rationalization list to 8 entries. Updated Relationship block clarifying the two-layer split: dataflow-identifier-safety.md \u00a7 4 guards force_drop at DDL-primitive layer; schema-migration.md \u00a7 7 guards force_downgrade at orchestrator layer. Both gate independently.",
        "VARIANT-PY ADDITION (NOT applied to rs target): variants/py/scripts/migrate.py \u2014 ~130 LOC Python migration starter template. Python source for downstream Python projects to copy as a migration scaffold. Imports from dataflow.adapters.dialect.DialectManager (kailash-py runtime API). Rust equivalents managed locally in kailash-rs BUILD repo.",
        "PIN: pyproject.toml \u2014 kailash-enterprise>=3.17.1 \u2192 >=3.18.0 (kailash-rs workspace version bump per Gate 1 verification).",
        "STRAY DIR REMOVAL: deleted .claude/agents/frameworks/.claude/learning/observations.jsonl (orphan from 2026-03-28 learning hook misfire; confirmed stray by Gate 2 inspection \u2014 single observations file with no commit-history backing).",
        "upstream.build_version 3.17.1 \u2192 3.18.0; sdk_packages map updated across workspace members (kailash-plugin-macros stays at 3.0.0 per its own pin)."
      ]
    },
    {
      "version": "2.9.3",
      "date": "2026-04-17",
      "summary": "Sync loom 2.8.12 \u2014 7 new globals (dataflow-classification, ci-runners, nexus-webhook-hmac, ui-backend-defense, ci-runner-troubleshooting, allowlist-extension-pattern, nexus-http-status-convention) + 3 extended globals (schema-migration \u00a7 3a force_drop, tenant-isolation UUID anti-optimization, orphan-detection \u00a7 2a crypto-pair round-trip + Rules 4-5 + Detection Protocol step 4) + 1 new rs variant skill (numbered-migration-scaffold). Drift fixes: commands/sync.md, scripts/hooks/log-triage-gate.js. Closes kailash-coc-claude-rs#51. SDK pin bumped 3.16.3 \u2192 3.17.1.",
      "changes": [
        "NEW RULE: rules/dataflow-classification.md \u2014 mutation-return redaction (create/upsert/bulk_create MUST route through apply_read_classification before returning; Tier 2 test per public method; scalar aggregate carve-out).",
        "NEW RULE: rules/ci-runners.md \u2014 self-hosted runner hygiene (toolchain setup per job, .env-change restart, post-fmt cascade protocol, runner auto-update disconnect recovery, binding-CI paths filter parity, no committed tokens, upload-artifact continue-on-error).",
        "NEW RULE: rules/nexus-webhook-hmac.md \u2014 HMAC verification MUST NOT be computed over re-serialized JSON inside a Nexus handler; workarounds are external middleware or reverse-proxy verification.",
        "NEW RULE: rules/ui-backend-defense.md \u2014 every enum/pluggable-value handler MUST allowlist server-side regardless of UI restrictions; three-layer defense (handler allowlist + validator shape check + type-strict model).",
        "NEW SKILL: skills/10-deployment-git/ci-runner-troubleshooting.md \u2014 recovery protocols (runner auto-update disconnect, rustup proxy recovery, .env change, post-fmt cascade triage, binding-CI path drift) + service-management reference (launchd / systemd).",
        "NEW SKILL: skills/13-architecture-decisions/allowlist-extension-pattern.md \u2014 built-in allowlist stays closed, register_<thing>() runs AFTER built-in downcast fails, identity-keyed storage, built-in wins over registered.",
        "NEW SKILL: skills/03-nexus/nexus-http-status-convention.md \u2014 {error, status} response shape, status code catalog (400/401/403/404/409/413/422/429/500/502/503), no bare strings / stack traces / mismatched codes.",
        "EXTENDED: rules/schema-migration.md \u2014 new \u00a7 3a 'Destructive downgrade() MUST Require force_drop=True' mirroring rules/dataflow-identifier-safety.md \u00a7 4.",
        "EXTENDED: rules/tenant-isolation.md \u2014 UUID anti-optimization guidance (tenant_id STAYS in key even when secondary key is per-tenant-unique UUID; 36-byte hedge against CVE-class refactor).",
        "EXTENDED: rules/orphan-detection.md \u2014 new \u00a7 2a 'Crypto-Pair Round-Trip MUST Be Tested Through The Facade' (encrypt/decrypt, sign/verify etc) + Rules 4-5 (API removal sweeps tests in same PR, pytest --collect-only is a merge gate) + Detection Protocol step 4 (collect-only sweep).",
        "NEW RS VARIANT SKILL: skills/02-dataflow/numbered-migration-scaffold.md \u2014 Python-binding starter scaffold for numbered migrations (migrations/ directory, discover(), scripts/migrate.py CLI) with force_drop gate per schema-migration.md \u00a7 3a.",
        "DRIFT FIX: commands/sync.md \u2014 restored from loom rs variant (USE-template downstream /sync flavor, was clobbered with loom-flavored Gate1+Gate2 version).",
        "DRIFT FIX: scripts/hooks/log-triage-gate.js \u2014 benchmarks/ exclusion re-synced from loom source (perf output is not session-actionable).",
        "PIN: pyproject.toml \u2014 kailash-enterprise>=3.16.3 \u2192 >=3.17.1 (kailash-rs workspace version bump).",
        "upstream.build_version 3.16.3 \u2192 3.17.1; sdk_packages map updated across workspace members."
      ]
    },
    {
      "version": "2.9.2",
      "date": "2026-04-15",
      "summary": "Sync loom 2.8.10 \u2014 DataFlow annotationlib fallback skill (Python 3.14 PEP 749 diagnostics for @db.model). Variant-only skill, retroactive authoring from kailash-rs 3.16.3 codify.",
      "changes": [
        "NEW SKILL: skills/06-python-bindings/annotation-resolution-fallback.md \u2014 when `typing.get_type_hints(cls)` raises NameError on a @db.model class, fall back to `annotationlib.get_annotations(cls, format=Format.FORWARDREF)` on 3.14+ for per-field diagnostics (which class, which fields, three common causes). On <3.14, raise a clearer RuntimeError than the raw NameError.",
        "UPDATED: skills/06-python-bindings/SKILL.md \u2014 index row added for annotation-resolution-fallback.md.",
        "upstream.build_version bumped to kailash-rs 3.16.3; sdk_packages map expanded to the full workspace member set (was kailash-enterprise only).",
        "PIN: pyproject.toml \u2014 kailash-enterprise>=3.15.2 \u2192 >=3.16.3 (the release that ships the DataFlow annotationlib fallback fix)."
      ]
    },
    {
      "version": "2.9.1",
      "date": "2026-04-15",
      "summary": "Sync loom 2.8.9 \u2014 rust-version-bump skill (cross-SDK parity with kailash-py's python-version-bump). Resolves kailash-rs#401.",
      "changes": [
        "NEW SKILL: skills/10-deployment-git/rust-version-bump.md \u2014 canonical procedure for raising rustc MSRV + adding/dropping CPython targets in the PyO3 wheel matrix. Includes PyO3 forward-compat flag pattern and maturin-action lesson.",
        "UPDATED: skills/10-deployment-git/SKILL.md \u2014 'Version Bumps' sub-section added referencing the new skill.",
        "build_version bumped to kailash-rs 3.16.3 (the release containing the skill)."
      ]
    },
    {
      "version": "2.9.0",
      "date": "2026-04-15",
      "summary": "Python 3.14 support \u2014 kailash-rs Rust SDK now tested and supported on Python 3.14 via PyO3.",
      "changes": [
        "PYPROJECT: Programming Language :: Python :: 3.14 classifier added.",
        "VERSION: minor bump 2.8.7 \u2192 2.9.0 to trigger downstream session-start update detection."
      ]
    }
  ]
}
